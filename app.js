
$('.toggle').toggles({
	drag: true,
	text:{
		on: "", // text for the ON position
		off:""  // and off
		},
	width: "50px" // width used if not set in css
	// if this is set to 'select' then the select style toggle will be used
});